# =============================================================================
# API CAEPI - Variáveis de Ambiente de Exemplo
# =============================================================================
# COPIE este arquivo para .env (desenvolvimento) ou .env.prod (produção)
# e preencha com os valores adequados para o seu ambiente.
# NUNCA adicione senhas ou chaves reais neste arquivo!
# =============================================================================


# --- Configurações da Fonte de Dados (FTP do MTE) ---------------------------
FTP_HOST=ftp.mtps.gov.br
FTP_ENDPOINT=portal/fiscalizacao/seguranca-e-saude-no-trabalho/caepi/
FTP_FILE_NAME=tgg_export_caepi.zip

# --- Configurações de Cache --------------------------------------------------
# Tempo em segundos que os dados ficam em cache.
CACHE_TIMEOUT=3600

